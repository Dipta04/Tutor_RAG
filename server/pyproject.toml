[project]
name = "tutor-rag"
version = "0.1.0"
description = "RAG tutor backend: FastAPI + MongoDB + Pinecone + Groq"
readme = "README.md"
authors = [
    { name = "Dipta04", email = "diptakarmakar38@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
